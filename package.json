{
  "name": "bun-hono-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "migrate": "bun run src/db/migrate.ts",
    "seed": "bun run src/db/seed.ts",
    "seed:extra": "bun run src/db/seed-extra.ts"
  },
  "dependencies": {
    "hono": "^4.12.14",
    "postgres": "^3.4.9",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "@types/bun": "^1.3.12"
  }
}
